# Tasks: Remove hf config bootstrap from Technical Architecture Spec

## 1. Update Technical Architecture Spec

- [x] 1.1 `openspec/specs/technical-architecture/spec.md` — remove `bootstrap|` from the `config.go` module layout comment
- [x] 1.2 `openspec/specs/technical-architecture/spec.md` — remove `bootstrap [env-name]` from the Cobra command tree
- [x] 1.3 `openspec/specs/technical-architecture/spec.md` — add `new [name]` under `env` in the Cobra command tree
